# World Config Overrides
# -----------------------
# - Copy this file to "config-overrides.yaml" in the same folder.
# - Use it for custom settings that should stay with this world/data folder.
# - It is loaded after "_datafiles/config-overrides.yaml".
# - Keep server-wide settings like hostnames, ports, and HTTPS in
#   "_datafiles/config-overrides.yaml".
# - The copied config-overrides.yaml file is ignored by git.

# Example world-specific override:
# FilePaths.DataFiles: "_datafiles/world/default"
